
let initialState = {
    bestFriendsData: [
        { id: 1, name: 'Andrew' },
        { id: 2, name: 'Sasha' },
        { id: 3, name: 'Sveta' }
    ]
}

const sidebarDataReducer = (state = initialState, action) => {
    return state;
}

export default sidebarDataReducer;